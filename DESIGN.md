# 03-视觉 设计文档：mini-ORB + mini_geom

## 参照
raulmur/ORB_SLAM2（5k★）、《视觉 SLAM 十四讲》配套代码 slambook2（6.7k★）、
ethz 视觉教程。对应计划里"从零实现 mini-ORB（FAST + BRIEF + 汉明匹配），再加相机标定、
PnP、光流各一个最小实现"。

**约束：全部纯 numpy，不 import cv2 / scipy / skimage，不调用任何现成特征或几何函数。**

## 模块一 · mini_orb —— 特征管线（已完成）

1. **FAST-9**：16 像素圆周的向量化实现（`sliding_window_view` 在圆周数组加倍后求 9 长窗口）。
2. **Harris 打分**：梯度结构张量 + 积分图盒滤波；按响应排序做半径 5 的贪心 NMS，取 top-N。
3. **主方向**：半径 15 圆形邻域的强度质心，`θ = atan2(m01, m10)`。
4. **steered BRIEF**：256 对采样点限制在半径 14 圆内（保证旋转后不出界），5×5 盒滤波平滑后
   按关键点方向旋转比较，输出 32 字节描述子。
5. **匹配**：`np.bitwise_count` 汉明距离 + 双向最近邻互验 + Lowe 比率检验。

## 模块二 · mini_geom —— 几何视觉（本阶段新增）

### `geometry.py` 共用原语
Rodrigues 正反变换（含 θ→π 的退化分支）、Frobenius 最近旋转正交化、Umeyama/Procrustes
刚体配准、带径向畸变的投影模型、迭代式去畸变、数值雅可比 + Levenberg-Marquardt。
LM 用对角缩放阻尼（`A + λ·diag(A)`），自适应接受/拒绝步长。

### `calib.py` 张正友平面标定
1. 每视图像素↔世界：**归一化 DLT** 求单应（各向同性归一化，条件数从 1e6 级降到 1e2 级）。
2. 闭式内参：由 `v_ij` 约束（`h1ᵀBh2=0`、`h1ᵀBh1=h2ᵀBh2`）SVD 解 `B = K⁻ᵀK⁻¹`，再反解
   `fx, fy, γ, u₀, v₀`。
3. 每视图外参：分解 `K⁻¹H`，按 `t_z > 0` 定尺度符号，SVD 正交化得 R。
4. 联合精化：LM 同时优化 `(fx, fy, γ, cx, cy, k1, k2)` 与每视图 6 个位姿参数。
   `free_distortion=False` 时只优化位姿——此时参数块里不含内参，避免雅可比出现零列导致 LM 假收敛。

### `pnp.py` 位姿求解
- `dlt_pose`：≥6 点线性解 3×4 投影矩阵。**尺度符号**由 `det(U Vᵀ)` 判定（λ<0 时 R 取负），
  比"看 t_z 再翻转"更严谨。
- `p3p`：**Grünert 型结式法**。把两组深度约束写成关于 v 的两个二次多项式，构造 4×4
  Sylvester 矩阵，用多项式算术展开行列式，`np.roots` 求 u 的根，回代求深度，Umeyama 得 R,t。
  排序依据是两式**相对**残差（绝对残差在 1e-12 量级无法区分候选）。
  *踩坑*：`numpy.polynomial` 的 `coef` 是升幂，`np.roots` 要降幂，顺序搞反等于求倒多项式的根
  （0.906 → 0.8928，1.5% 的系统偏差）。
  *本质*：3 点最多 4 组解且都能精确重投影，**必须**用第 4 点或先验消歧。
- `refine_pose` / `solve_pnp`：LM 精化；P3P 每个候选都拿全部对应点精化后取 rms 最小者。
- `ransac_pnp`：P3P 作最小解算器 + 3px 内点投票 + 内点重拟合 + 二次投票。

### `flow.py` 光流
- 金字塔：可分离高斯（σ=1.0）后 2 倍抽取。
- `lk_solve`：**每次迭代在当前 warp 位置重建 Hessian**（原始前向加性形式）。若像常见写法那样
  把 Hessian 冻结在模板位置，收敛半径不足 1px，金字塔粗层直接失效。
  *踩坑*：H 与 b 的行序必须与位移向量 `(dy, dx)` 一致，否则牛顿步装配错位、迭代发散。
  *踩坑*：patch 越界判断要用**半径** `win//2` 而非整窗宽 `win`，否则最粗层所有点被静默跳过。
  *踩坑*：纯牛顿步在粗层会过冲，需按 SSD 下降做回溯（halving）。
- `min_safe_margin(levels, win)`：给出"每层都不越界"的最小边距——点在粗层的坐标是
  `m/2^(levels-1)`，必须仍大于 patch 半径。
- `affine_lk`：6 参数（4 仿射 + 2 平移）逐点估计，同样多尺度 + 回溯 + Hessian 条件数守卫。
  单层收敛域约 1°，5° 旋转必须靠金字塔。

## 端到端验证：`demo_pipeline.py`
合成场景：z=0 平面贴噪声纹理，第一帧为世界参考，第二帧位姿真值已知。
逐像素 ray-cast 渲染两帧 → ORB 检测/描述 → 汉明匹配 → 反投影到平面得 3D 点 →
RANSAC-PnP 求第二帧位姿 → 与真值对比。

## 验证结果（合成真值，全部离线）

| 模块 | 指标 | 结果 |
|------|------|------|
| 标定（无噪声） | fx/fy 相对误差、主点误差、rms | <1e-3、<0.5px、1e-5 px 级 |
| 标定（0.3px 噪声） | fx 相对误差 / 主点 / k1 / rms | 0.186% / 0.43px / -0.1802（真值 -0.18）/ 0.276px |
| 去畸变 | 去畸变后针孔重投影残差 | 0.0000 px（原始 47.29 px） |
| DLT PnP（无噪声） | 旋转 / 平移误差 | 0° / 2e-14 |
| DLT+LM（0.5px 噪声） | rms / 旋转误差 | 5.27 → 0.45 px / 0.24° |
| P3P（3 点无噪声） | 候选数 / 最优旋转误差 | 4 个候选 / 0° |
| RANSAC PnP（30% 粗差） | 内点数 / 旋转误差 | 56/80 / 0.094°（普通最小二乘 179.3°） |
| LK 平移（2.3px） | 平均端点误差 | 0.044 px |
| LK 金字塔（12.6px） | 单层 vs 4 层 | 15.87 px vs **0.026 px** |
| 仿射 LK（5°+1.06×） | 中位相对误差 | 0.0030（12 点全部 <5%） |
| 端到端 demo | 匹配内点率 / RANSAC / 位姿误差 | 94% / 195-208 / 0.094°、0.0007 |

## 目录
```
03-vision/
├── DESIGN.md            本文档
├── README.md
├── demo_pipeline.py     端到端：渲染→ORB→匹配→反投影→RANSAC-PnP
├── mini_orb/            特征管线（FAST/BRIEF/匹配）
│   ├── features.py
│   └── test_features.py
└── mini_geom/           几何视觉（本阶段新增）
    ├── geometry.py      Lie/配准/投影/LM 原语
    ├── calib.py         张正友标定
    ├── pnp.py           DLT / P3P / 精化 / RANSAC
    ├── flow.py          金字塔 LK + 仿射 LK
    └── test_minigeom.py 11 项自校验测试
```
